README:

Activity 1 info:
The program will only work with the default.json that was provided.

Activity 2 info:
The program will only work with the default.json that was provided.
the 'log' function will only track the current conversation, so it will be cleared when 'quit' is entered.
